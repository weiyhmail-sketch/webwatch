# WebWatch 操作手册（小白版，照着一步步做）

> 本手册教你：从零把面板跑起来 → 在 **paper 模拟盘** 上验证每个功能 → 日常使用。
> **全程默认 paper（模拟金，不花真钱）。切 live 实盘在最后一章，验证通过前不要做。**
>
> 约定：`这种字` = 在终端里输入的命令；【按钮】= 面板上点的东西。
> 终端 = Mac 的「终端」App（按 ⌘+空格 搜 "终端" 打开）。

---

## 0. 整体流程一图

```
① 启动 IB Gateway(模拟盘) → ② 测试连接 → ③ 启动面板 → ④ 浏览器打开面板
   → ⑤ 加自选看报价 → ⑥ 逐项验证下单/撤单/全平 → ⑦ 日常使用
```
每次要用，只需重复 ①③④。②⑤⑥ 第一次做就行。

---

## 1. 准备工作（一次性，只做一次）

你需要三样东西，缺一不可：
1. **IBKR 账户**，且开通了**模拟账户（Paper）**。账户号长这样：`DU` 开头（模拟）/ `U` 开头（实盘）。
2. **IB Gateway**（或 TWS）已装在这台 Mac 上。没装的话，去 IBKR 官网下载 "IB Gateway"，
   安装是通用步骤（可让 ChatGPT 教你装），装完能用 IBKR 账号登录即可。
3. **本项目代码**已在：`/Users/weiyuhang/Desktop/AI工程项目/webwatch`（你现在就有）。

> 💡 IB Gateway vs TWS：两者都行，本手册以 **IB Gateway** 为例（更轻量）。TWS 同理，端口设置一样。

---

## 2. 启动 IB Gateway（模拟盘）—— 每次用都要先开

1. 打开 **IB Gateway**。
2. 登录界面左边选 **Paper Trading（模拟交易）**，输入你的 IBKR 用户名密码，登录。
3. 登录后，点菜单 **Configure（齿轮）→ Settings → API → Settings**，确认：
   - ✅ 勾选 **Enable ActiveX and Socket Clients**（启用 API）
   - ✅ **Socket port** 填 **4002**（这是模拟盘端口，务必是 4002）
   - ⬜ **取消勾选 Read-Only API**（只读会导致不能下单）
   - **Trusted IPs（信任 IP）** 里加上 `127.0.0.1`
   - 点 **OK / Apply** 保存。
4. 让 IB Gateway 一直开着别关。

> ⚠️ 端口必须 4002。如果你不小心连到 4001，那是**实盘**端口——本面板代码有防呆会拒绝，但你自己也要记住：**4002=模拟，4001=实盘**。

---

## 3. 配置 webwatch（一次性）

打开终端，逐行复制运行（一次一行）：

```
cd "/Users/weiyuhang/Desktop/AI工程项目/webwatch"
```
```
uv sync
```
（第一次会装依赖，可能要几分钟，正常。）

然后创建你的模拟盘配置文件：
```
cp config/secrets.env.example config/secrets.paper.env
```
用「访达」打开 `config/secrets.paper.env`（或 `open config/secrets.paper.env`），
把里面这行的账户号改成你**真实的模拟账户号**（DU 开头）：
```
WEBWATCH_IBKR_ACCOUNT=DU你的账户号
```
> 其他几行（HOST=127.0.0.1、PORT=4002、CLIENT_ID=11）保持不动。
> 这个文件存了账户号，**不会进 git、不会外泄**，放心填。
> （其实账户号不填也能自动探测，但填上更稳。）

---

## 4. 测试连接（确认能连上 Gateway）

确保 IB Gateway 已登录模拟盘（第 2 步）。终端运行：
```
uv run python scripts/check_connection.py
```

**你应该看到**（数字是你模拟账户的）：
```
[webwatch] ✅ 已连接 paper Gateway (127.0.0.1:4002)
[webwatch] 账户摘要:
    NetLiquidation     1027470.93
    ...
```
看到 `✅ 已连接` + 账户净值 = 成功。如果报错，跳到第 7 章排查表。

---

## 5. 启动面板 + 界面讲解

终端运行（IB Gateway 要开着）：
```
uv run uvicorn webwatch.app:app --port 8000
```
看到 `Uvicorn running on http://127.0.0.1:8000` 后，**浏览器打开**：
```
http://127.0.0.1:8000
```
> 关面板：回到那个终端窗口，按 `Ctrl + C`。

### 界面各部分（从上到下）
- **顶栏左边**：`PAPER`（绿色=模拟盘，放心）。旁边圆点绿=已连接。
- **时段徽章**：`时段:常规盘/盘前/盘后/夜盘/休市`（绿=常规盘，琥珀=时段外，灰=休市）。
- **账户**：净值/现金/购买力。
- **顶栏右边**：【撤全部挂单】【市价全平（红）】【重连】。
- **下单区**：代码、数量、限价/参考价、止盈下拉+数值、止损下拉+数值、【预览】【限价买入】【市价买入】。
- **报价区**：输入代码【添加】到自选；右边「行情类型」下拉（实时/冻结/延迟/延迟冻结）。
- **持仓表 / 挂单表**：实时刷新。

### 止盈/止损的三种模式（下拉里选）
- `%`：百分比，填 `0.002` = +0.2%。
- `+$/股`：每股价格偏移，填 `1` = 成交价 +$1。
- `盈利$`：总盈利金额，填 `100` = 赚到 $100 就止盈（按手数自动反推）。
止损下拉同理（%、-$/股、亏损$）。

### 键盘热键（光标不在输入框时）
`b`=市价买入　`l`=限价买入　`f`=市价全平　`c`=撤全部挂单

---

## 6. M6 验证清单（逐项操作，每项都有"预期"）

> 全部在 **paper** 上做。建议**美股常规盘时段**（北京时间见第 8 章）做第 ①–⑤ 项，
> 这样报价会跳动、市价能成交。盘前/盘后/夜盘做第 ⑥ 项。

### ① 连接 & 账户（任意时间可做）
- 打开面板。**预期**：顶栏绿点「已连接」、`PAPER` 绿、账户净值显示出来。

### ② 报价（建议盘中）
1. 下单区下面的报价区，输入 `AAPL`，点【添加】。
2. **预期**：AAPL 出现在报价表，盘中会看到买一/卖一/最新在跳。
3. 如果全是 `—`（没数据）：看顶部有没有红字 `IB 10197...`。有的话 → 你的实时行情被别处占用了。
   把报价区右边「行情类型」下拉切到 **延迟**，过几秒应能看到延迟报价（代码旁会标"延迟"）。
   （详见第 7 章 10197 那条。）

### ③ 限价买入（盘中，会真挂模拟单）
1. 代码 `AAPL`，数量 `10`，限价填一个**远低于现价**的数（比如现价 $200，你填 `50`）——
   这样不会真成交，只是挂着，安全验证。
2. 止盈 `%` `0.002`，止损 `%` `0.004`。
3. 点【预览】。**预期**：下方显示「止盈 / 止损 / 金额 / 扣费净盈亏」。
4. 点【限价买入】→ 弹窗确认 → 确定。
5. **预期**：下方显示「✓ 已下单」+ 三个单号；**挂单表**出现 3 条：
   `limit long`(母单)、`limit short`(止盈)、`stop short`(止损)。
6. 验证完点【撤全部挂单】→ 确认。**预期**：挂单表清空。

### ④ 市价买入 + 自动挂止盈止损（盘中，会真成交模拟单！）
> ⚠️ 这步会**真的成交一笔模拟买入**并自动挂止盈止损。用小数量。
1. 代码 `AAPL`，数量 `10`，止盈 `%` `0.002`，止损 `%` `0.004`。
2. 点【市价买入】→ 确认。
3. **预期**：下方显示「✓ 市价成交 10 股 @ 实际成交价 — 止盈 X / 止损 Y (OCA ...)」；
   **持仓表**出现 AAPL 10 股；**挂单表**出现 2 条（止盈限价 + 止损 stop）。
4. 这说明核心功能 OK：市价成交后**用真实成交价**算出止盈止损并自动挂上。
5. 清理：点【市价全平】→ 确认。**预期**：持仓平掉、挂单清空。

### ⑤ 撤单 / 全平 / 热键（盘中）
- 随便挂一单，按键盘 `c` → 确认 → 挂单清空（= 撤全部挂单）。
- 有持仓时按 `f` → 确认 → 全平。

### ⑥ 时段外（盘前/盘后/夜盘）验证
> 在美股盘前/盘后/夜盘时段做（时段徽章显示「盘前/盘后/夜盘」）。
1. **限价买入**：同第③步。**预期**：能挂出，挂单的止损会是 **stop-limit**（时段外普通 stop 不触发，
   系统自动转）。下单结果会标 `outsideRth`。
2. **市价买入**：点【市价买入】→ 确认弹窗会提示「时段外：市价不可用，将自动转为激进限价 bracket」。
   **预期**：下方显示「✓ 时段外已挂激进限价 bracket」。
3. **夜盘特别注意**：夜盘只有部分股票可交易、流动性薄。**第一次夜盘下单，建议用很小数量、远价限价**，
   先看能不能挂出、会不会成交（夜盘路由是否生效需要实测）。挂完用【撤全部挂单】清掉。
4. **休市（周末）**：点任何下单都会被拒「当前休市，不接单」——这是对的，防止挂单放到下周。

### 验证通过标准
①②③④⑤ 全部符合预期 = paper 核心验证通过。⑥ 视你要不要用时段外/夜盘再测。

---

## 7. 常见问题排查表

| 现象 | 原因 | 怎么办 |
|---|---|---|
| `check_connection` 报 "Gateway not reachable / 4002" | IB Gateway 没开 / 没登模拟盘 / 端口不是 4002 | 回第 2 步：开 Gateway、登 Paper、API 端口设 4002 |
| 报 "clientId ... in use / 326" | clientId 11 被别的程序占用 | 改 `config/secrets.paper.env` 里 `WEBWATCH_IBKR_CLIENT_ID=12`（换个数字）|
| 面板报价全是 `—`，顶部红字 `IB 10197` | 实时行情被你的另一个 IBKR 会话占用 | ①关掉其他 IBKR 登录（手机 App/TWS/另一个 Gateway）；②或把「行情类型」切「延迟」看免费延迟行情 |
| 报价 `—` 但无红字，且是美股盘后/周末 | 休市，本来就没实时跳动 | 正常；盘中再看，或切「延迟/冻结」看上一次价 |
| 面板打不开 / 浏览器转圈 | uvicorn 没起来 / 端口被占 | 看终端有没有报错；换端口：`uv run uvicorn webwatch.app:app --port 8001` 然后开 8001 |
| 下单显示「风控拒绝」 | 触发了保护（金额超上限/单笔亏损超 NAV 0.5% 等）| 看红字原因，减小数量/金额。这是保护你的，不是 bug |
| 顶部琥珀条「请求 live 但未设 ...」 | 你设了 live 但没设确认变量 | 想用模拟盘就别设那些变量，正常启动即可 |

---

## 8. 美东时段 ↔ 北京时间对照（夏令时，约 6–11 月）

| 美东时段 | 美东时间(ET) | 北京时间(约) |
|---|---|---|
| 盘前 | 04:00–09:30 | 16:00–21:30 |
| **常规盘 RTH** | 09:30–16:00 | **21:30–04:00(次日)** |
| 盘后 | 16:00–20:00 | 04:00–08:00 |
| 夜盘 | 20:00–04:00 | 08:00–16:00 |

> 冬令时（约 11–3 月）整体往后推 1 小时（常规盘 22:30–05:00 北京）。
> 以面板上的**时段徽章**为准最准。

---

## 9. 切到 live 实盘（⚠️ paper 验证通过 + 复核 PASS + 你确认后再做）

> **不要急着切。** 先在 paper 用真实操作跑顺 1–2 天，确认每步都符合预期，再考虑 live。

1. 创建实盘配置：`cp config/secrets.env.example config/secrets.live.env`，
   编辑 `config/secrets.live.env`：账户号填你的**实盘账户（U 开头）**，端口改 **4001**。
2. IB Gateway **退出模拟盘、改用实盘登录**（登录界面选 Live），API 端口确认 4001。
3. 启动面板时必须**同时**带两个开关（缺一会自动退回 paper，这是安全互锁）：
   ```
   WEBWATCH_ENV=live WEBWATCH_LIVE_CONFIRM=YES uv run uvicorn webwatch.app:app --port 8000
   ```
4. **预期**：面板顶部出现**红色脉冲横幅「🔴 LIVE 实盘」**，env 徽章变红。每笔下单弹窗都带「🔴 LIVE」。
5. **首周用最小数量**。live 单笔上限默认 $20000（可在 `config/panel.yaml` 调小，比如先设 500 更稳）。
6. 任何时候想回模拟盘：关掉面板，去掉那两个环境变量，重新 `uv run uvicorn ...` 即可（默认就是 paper）。

---

## 10. 紧急操作 & 每日收尾

- **紧急**：行情不对/想立刻离场 → 点【市价全平】（或按 `f`）→ 会先撤所有挂单再市价平所有持仓。
- **只撤挂单不平仓**：【撤全部挂单】（或按 `c`）。
- **每日收尾**：超短线不留隔夜的话，收盘前用【市价全平】清干净；关面板（终端 Ctrl+C）；
  IB Gateway 可关可不关。
- **每次重新开工**：开 IB Gateway(模拟盘) → 终端 `cd` 到项目 → `uv run uvicorn webwatch.app:app --port 8000` → 开浏览器。

---

有任何一步对不上"预期"，把终端/面板上的报错截图或文字发我，我帮你定位。
